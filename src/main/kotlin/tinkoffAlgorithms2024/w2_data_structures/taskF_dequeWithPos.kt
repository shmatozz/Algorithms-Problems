package tinkoffAlgorithms2024.w2_data_structures

/*
import sys

n = sys.stdin.readline()
popped = 0
deque = []
positions = [0] * 100001

for i in range(int(n)):
    op = sys.stdin.readline().split()
    if op[0] == '1':
        el = int(op[1])
        deque.append(el)
        positions[el] = len(deque) - 1 + popped
    elif op[0] == '2':
        del deque[0]
        popped += 1
    elif op[0] == '3':
        deque.pop()
    elif op[0] == '4':
        el = int(op[1])
        sys.stdout.write(f"{positions[el] - popped}\n")
    elif op[0] == '5':
        sys.stdout.write(f"{deque[0]}\n")

 */

fun main() {
    println("Python solution above")
}