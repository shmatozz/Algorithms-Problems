package tinkoffAlgorithms2024.w2_data_structures

/*
import sys

n = int(sys.stdin.readline())
deque = []

for i in range(int(n)):
    op = sys.stdin.readline().split()
    if op[0] == '+':
        el = int(op[1])
        deque.append(el)
    elif op[0] == '*':
        el = int(op[1])
        deque.insert(len(deque) // 2 + len(deque) % 2, el)
    elif op[0] == '-':
        sys.stdout.write(f"{deque[0]}\n")
        del deque[0]

 */

fun main() {
    println("Python solution above")
}